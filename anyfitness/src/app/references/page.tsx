const TEST_URI = 'http://localhost:3000/api';
export default async function ReferencesPage() {
  //   const references = await fetch(`${TEST_URI}/references`, {
  //     cache: 'no-store',
  //   }).then((res) => res.json());
  return <></>;
}
